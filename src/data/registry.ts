import { satellite } from './cases/satellite';
import type { CaseStudyData } from './schema';
export { satellite };
/** Register once: routes and project cards derive from this list. */
export const caseStudies: CaseStudyData[] = [satellite];
